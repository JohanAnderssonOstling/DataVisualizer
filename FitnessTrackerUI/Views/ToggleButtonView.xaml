<UserControl x:Class="DataVisualizerUI.Views.ToggleButtonView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DataVisualizerUI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>

        </Grid.RowDefinitions>
        <ToggleButton x:Name="WeightButton"  Grid.Row="0" IsChecked="True" Content="Weight"/>
        <ToggleButton x:Name="BodyFatPercentageButton"  Grid.Row="1" IsChecked="True" Content="Body Fat(%)"/>
        <ToggleButton x:Name="BodyMassButton"  Grid.Row="2" IsChecked="True" Content="Body Fat(kg)"/>
        <ToggleButton x:Name="MusclePercentageButton"  Grid.Row="3" IsChecked="True" Content="Muscle(%)"/>
        <ToggleButton x:Name="MuscleMassButton"  Grid.Row="4" IsChecked="True" Content="Muscle(kg)"/>
    </Grid>
</UserControl>
